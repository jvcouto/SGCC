version: "3.1"

services:
  database:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: postgres
    ports:
     - '5432:5432'
    volumes:
      - /src/data/database/data:/var/lib/postgresql/data
